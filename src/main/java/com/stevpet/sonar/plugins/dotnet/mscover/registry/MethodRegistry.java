package com.stevpet.sonar.plugins.dotnet.mscover.registry;

import com.stevpet.sonar.plugins.dotnet.mscover.model.MethodModel;

public class MethodRegistry {
    public void add(MethodModel method) {
        // TODO Need to determine how we're going to organize the methods. Most likely one will want to
        // find the method by assembly/name, but will add that once we're there
        
    }

}
